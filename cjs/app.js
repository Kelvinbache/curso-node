/*conceptos de consola
 no pdremos hacer una console.log window osea no podremos ver la ventana
 console.log(globalThis) es una variable globar  de la aplicacion 
*/

// Patron de diseno (forma clasica que no debemos usar)
function suma(a,b){
  return a + b;
}

module.exports = {
    suma
}